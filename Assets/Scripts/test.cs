using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class test : MonoBehaviour
{
    // Update is called once per frame
    void Update()
    {
        // GameObject.Find("AidmedSensor").GetComponent<Connect>().ReadBaseHeartRate();
        // GameObject.Find("AidmedSensor").GetComponent<Connect>().ReadHeartRate();
        // GameObject.Find("AidmedSensor").GetComponent<Connect>().ReadDiff();
    }
}
